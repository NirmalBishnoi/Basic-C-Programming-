#include <stdio.h>
int main(){
	int a,s,surfacearea;//a=area of one side  s=number of side
	printf ("enter the value of a,s");
	scanf ("%d%d",&a,&s);
	surfacearea=a*s;
	printf("Surface area of R.polyhedron%d",surfacearea);
}
